<% @tasks.each do |task| %>
  <li>
    <%= task.done ? '[ x ]' : '[ ]' %>
    <%= task.title %> - <%= link_to "Mark as done", edit_task_path(task), remote: true, class: "btn btn-primary" %>
  </li>
<% end %>
